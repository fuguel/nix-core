This can be used as a template for beginner NixOS users to start building their flake. 

It is a multi-host flake, with a common.nix and modules imported into specific hosts.

Its basically a starter template that includes a working Mail stack using NeoMutt with home manager

Hyprland imported into hosts with home manager, managing waybar and the hyprland.conf
